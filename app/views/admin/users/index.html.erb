<h1>Listing users</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Email</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @admin_users.each do |admin_user| %>
      <tr>
        <td><%= admin_user.email %></td>
        <td>
          <%= link_to 'Show', admin_user_path(admin_user), class: "btn-small btn-info" %>
          <%= link_to 'Edit', edit_admin_user_path(admin_user), class: "btn-small btn-primary" %>
          <%= link_to 'Destroy', admin_user_path(admin_user), method: :delete, data: { confirm: 'Are you sure?' }, class: "btn-small btn-danger" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br />

<%= link_to 'New User', new_admin_user_path, class: "btn" %>
